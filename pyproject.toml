[project]
name = "openwhisper"
version = "0.1.0"
description = "OpenWhisper MVP CLI"
readme = "README.md"
requires-python = ">=3.13"

[project.scripts]
openwhisper = "openwhisper.cli:main"

[tool.uv]
package = true
